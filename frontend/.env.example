# Base URL for API requests. In local dev, leave as /api to use the Vite proxy.
# For production behind nginx (docker-compose), use /api.
# For direct backend access without a proxy, set the full backend URL (e.g. http://localhost:8000).
VITE_API_BASE_URL=/api
